host: 10.10.10.197
port: 443

url: "https://randacek.dev"
baseurl: ""

markdown: kramdown
highlighter: rouge

include: ['.htaccess']

exclude: [
    "README.md",
    "run",
    "runlocal",
    "fullchain.pem",
    "privkey.pem",
]
